import { type ReactNode } from 'react';

// Stub for Milestone 2 — replaced in Task 3.6 with the real gateway + store
// context (useGateway / useRuntimeStore / useAgentStatuses).
export function RuntimeProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
