{
  "name": "trading-office",
  "version": "0.1.0",
  "private": true,
  "description": "AI Office Shell / Control Room — Phase 0: Office Visual Builder Kit",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "dev": "npm run dev -w trading-lab-research-floor",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "generate": "npm run generate -w trading-lab-research-floor",
    "preview": "npm run preview -w trading-lab-research-floor"
  },
  "engines": {
    "node": ">=20.19"
  }
}
